# Picovoice Porcupine - required for wake word detection
# Get your key at https://console.picovoice.ai/
PICOVOICE_ACCESS_KEY=your_access_key_here

# Optional: path to custom "Doraemon" wake word model (.ppn file)
# Train at https://console.picovoice.ai/ and download the .ppn for your platform.
# Use a relative path (e.g. doraemon/Dora-e-mon_pt_android_v4_0_0/..._android_....ppn on Termux)
# so the same repo works on Android and desktop. Leave empty to use built-in "computer" keyword.
WAKE_WORD_MODEL_PATH=

# Wake word phrase for Termux speech-recognition fallback (default: doraemon)
# On Termux, the bot listens via Google Speech Recognition and triggers when
# this word is heard. Case-insensitive.
WAKE_WORD=doraemon

# Optional: extra wake phrases in YOUR language (comma-separated). Use when "Doraemon"
# is not in your language and is often misheard. E.g. "ok música, ó música" (Portuguese).
# WAKE_PHRASES=ok música, ó música

# Language for spoken feedback: "Yes?", "Stopped", "Goodbye", etc. (e.g. pt, en, es).
FEEDBACK_LANGUAGE=en

# Language for recognizing the SONG NAME. Set to en-US if you say song titles in English;
# leave empty to use SPEECH_LANGUAGE for both wake word and song name.
# PHRASE_LANGUAGE=en-US

# Termux: if the mic is not detected, run "pactl list sources short" and set
# PULSE_SOURCE to the source name (e.g. "default" or the mic device).
# PULSE_SOURCE=default

# Speech recognition language for the WAKE WORD (e.g. pt-PT for Portuguese).
# Set to your language so "Doraemon" or WAKE_PHRASES are recognized.
SPEECH_LANGUAGE=en-US

# Termux: set to 1 to see recording diagnostics (opus size, PCM size, "no speech", etc.)
# TERMUX_DEBUG=0

# Termux: custom path for termux-microphone-record output if default (cache/termux_rec.opus) stays empty.
# Example: $HOME/storage/downloads/doraemon_rec.opus (run termux-setup-storage first).
# TERMUX_RECORD_PATH=

# Voice "turn off": after wake word, say "go to sleep", "goodbye", or "stop listening" to exit (no taps).
# Optional: speech recognition timeouts (seconds)
LISTEN_TIMEOUT=5
PHRASE_TIME_LIMIT=10
