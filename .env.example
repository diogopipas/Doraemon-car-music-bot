# Required on desktop: Picovoice access key for Doraemon wake word.
# Get a key at https://console.picovoice.ai/
PICOVOICE_ACCESS_KEY=your_key_here

# Required on desktop: path to Doraemon wake word model (.ppn file).
# Train "Doraemon" at https://console.picovoice.ai/ and download the .ppn for your platform
# (Android for Termux, macOS/Linux/Windows for desktop). Relative to project root.
WAKE_WORD_MODEL_PATH=doraemon/Dora-e-mon_pt_android_v4_0_0/Dora-e-mon_pt_android_v4_0_0.ppn

# Wake word for Termux fallback (only "Doraemon" triggers the bot; this is the recognized word).
WAKE_WORD=doraemon

# Language for spoken feedback. pt = Doraemon persona (Portuguese with Spanish accent: "Si?", "Ah claro amigo!", etc.).
FEEDBACK_LANGUAGE=pt

# Language for recognizing the SONG NAME. Set to en-US if you say song titles in English.
# PHRASE_LANGUAGE=en-US

# Speech recognition language for the wake word (e.g. pt-PT for Portuguese).
SPEECH_LANGUAGE=pt-PT

# Optional: seconds to wait for speech after "Si?" (default 3). Lower = faster.
# LISTEN_TIMEOUT=3
# PHRASE_TIME_LIMIT=10

# Termux: if the mic is not detected, run "pactl list sources short" and set PULSE_SOURCE.
# PULSE_SOURCE=default

# Termux: set to 1 for recording diagnostics.
# TERMUX_DEBUG=0

# Optional: pre-recorded clips for instant response. Add si.mp3 and ah_claro_amigo.mp3
# (Spanish accent, Portuguese) to doraemon/assets/ for fastest "Si?" and "Ah claro amigo!".
