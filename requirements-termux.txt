# Termux (Android) dependencies
# pvporcupine, pvrecorder, and PyAudio are NOT needed on Termux.
# Wake word detection uses sox + Google Speech Recognition instead.
SpeechRecognition>=3.10.0
yt-dlp>=2024.1.0
gTTS>=2.5.0
python-dotenv>=1.0.0
